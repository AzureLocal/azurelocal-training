export default function HomePage() {
  return (
    <main style={{ padding: '2rem', fontFamily: 'system-ui, sans-serif' }}>
      <h1>Azure Local Training — AI Tutor</h1>
      <p>
        This is the scaffolded landing page for the AI tutor. The full module selector,
        chat UI, and Claude API integration will land in subsequent commits.
      </p>
      <p>
        See <code>app/README.md</code> for the planned architecture and roadmap.
      </p>
    </main>
  );
}
